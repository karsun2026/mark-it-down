-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.3.0
ruff==0.15.4

# Fixture generation only - never imported by runtime code.
python-docx==1.2.0
fpdf2==2.8.8

# Release-test harness only (scripts/release_test_local.py): samples RSS
# across the process tree, including the A1.6 child. Never imported at runtime.
psutil==7.2.2
